# Supabase Connect > Transaction pooler URI (port 6543)
SUPABASE_DATABASE_URL=

# Protects the server-only admin overview endpoint
ADMIN_API_KEY=

# Optional local/custom-domain override for social metadata
NEXT_PUBLIC_SITE_URL=http://localhost:3000
